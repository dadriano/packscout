# Task: Review and Decide Access Requests in the Admin

**ID:** closed-beta-access/010
**Depends on:** closed-beta-access/003, admin-tools/003
**Blocks:** closed-beta-access/011
**Estimated scope:** medium
**Status:** done

## Objective

Administrators can see who is waiting for beta access, approve or decline them, and revoke access from someone already admitted — all from the admin's product-users area, with confirmation and an audit trail.

## Context

The allowlist handles people who were invited in advance. This handles everyone else: the people who found PackScout, signed in, and are now waiting for a human to decide.

The admin already has a product-users ledger showing every sign-up with identity, sign-in source, standing, timestamps, and saved-item counts. This task adds the admission dimension to it — the access state and how it was decided — plus a queue view of what is waiting and the three decision actions. The approved reference admin does exactly this as a reversible status control on the user row, with no hard delete anywhere in the flow.

The distinction that has to survive contact with the interface is between *not admitted yet* and *suspended*. They look similar in a badge and mean completely different things: one is a person waiting at the door, the other is an account that was disciplined. An operator must be able to tell them apart at a glance and act on the right one.

## Requirements

- The product-users ledger shows each user's access state and how it was decided — automatically by the allowlist, by an operator, or not yet decided — alongside the standing already displayed.
- Access state and standing are visually distinguishable at a glance using the admin's existing status and badge patterns; a waiting account never looks like a suspended one.
- A review queue surfaces identities awaiting a decision, oldest request first so nobody is buried, with bounded pagination.
- How many identities are waiting is visible without paging the queue — in the navigation or the page header — so operators know work is there.
- Approve, decline, and revoke actions are available from the row or detail view, guarded by the manage-product-users permission. Each requires explicit confirmation stating what changes for that person.
- Outcomes update the row immediately and converge under repeated or concurrent action: a conflict resolves to the authoritative current decision and says so, never an opaque failure.
- Every decision produces an audit record per the admin's conventions: acting operator, target subject, action, previous and resulting decision, timestamp, outcome — with no personal data beyond audit-relevant identifiers.
- Decisions are reversible in both directions and nothing in the flow deletes a user or their data.
- Data operators see no decision controls and receive the standard forbidden outcome on direct requests; anonymous requests receive the standard unauthenticated outcome.
- If the product-backend integration is unavailable, the area degrades to a clear, non-destructive error state; no integration credential and no raw backend error body reaches the browser.
- Loading, empty queue, error, and forbidden states are covered accessibly at desktop and narrow widths, reusing the admin's existing shell, ledger, confirmation, and toast patterns.

## User-Facing Behavior

An administrator sees a waiting count in the admin, opens the review queue, and works through it oldest-first. Each row shows who the person is, how they signed in, when they arrived, and that they are awaiting review. Approving asks for confirmation, then updates the row and admits the person — who, if they are sitting on the waiting screen, is let straight into the product. Declining and revoking work the same way with their own consequences spelled out. A data operator sees the users ledger without any decision controls.

## Interface Contract

- Protected admin endpoints for approve, decline, and revoke keyed by the stable subject, guarded by the manage-product-users permission, backed by the operator decision operations from closed-beta-access/003, returning the resulting decision for immediate display.
- A protected queue listing filtered by access state with the admin's usual pagination contract, plus a bounded waiting count for the navigation or header.
- The users ledger rows carry access state and decision provenance.
- The admin server owns the integration credential; it never crosses to the browser.

## Acceptance Criteria

- [x] The ledger shows access state and decision provenance, visually distinguishable from standing.
- [x] The review queue lists waiting identities oldest-first with bounded pages, and the waiting count matches the queue.
- [x] Approve, decline, and revoke each work with confirmation, update the row immediately, and emit an audit record.
- [x] Repeated or concurrent decisions converge to the authoritative decision and report it rather than failing opaquely.
- [x] Anonymous and data-operator requests receive the standard unauthenticated and forbidden outcomes, and data operators see no decision controls.
- [x] Integration failure degrades to a clear non-destructive state with no credential or raw backend error in the browser.
- [x] Loading, empty-queue, error, and forbidden states render accessibly at desktop and narrow widths without page-level overflow.

## Verification

Admin route behavior tests prove the permission matrix, each decision's outcome and convergence under repeat and concurrent action, audit emission, queue ordering and pagination bounds, count accuracy, and sanitized integration-failure mapping; page-level tests cover the queue, the access-state presentation, and the absence of decision controls for data operators. Layout is checked at desktop and narrow widths in both themes with no page-level horizontal overflow. The admin lint, typecheck, test, and build commands exit 0.

## Spec Compliance

- Related specs reviewed: `.tasks/closed-beta-access/_index.md` (reference-admin porting rule: reversible status control on the user row, no hard delete; admission kept apart from standing); closed-beta-access/003's Spec Compliance — this task consumes exactly the surface it documents (`POST /admin/product-users/access/{approve,decline,revoke}` with `{subject, operatorId}` → `decided`/`nothing_to_decide`, `/access/queue` with `accessState` + `paginationOpts` → directory rows with `queueTruncated`, `/access/queue-count` → bounded `{count, truncated}`, and `access` on every directory record) and emits the audit records 003 deferred here; closed-beta-access/009's Spec Compliance (the sibling pattern: integration client shape, `createRecordReferencer` pseudonymized audit, session-stamped operator, route guard + CSRF, page/ledger/confirm/toast reuse); `docs/admin-feature-baseline.md`, `docs/ui-layout-standard.md`; admin-tools/003's product-users surface, which this task extends in place.
- Alignment: The shared vocabulary grows in `packages/contracts/src/product-users.ts` (no new permission — the existing `product_users:view`/`product_users:manage` split already covers reads and decisions): access states, provenance kinds, the browser-facing three-field `ProductUserAccessDecision`, the reason-paired `ProductUserEffectiveAccess` union, bounded strict request schemas for the queue and decisions, and the shared operator copy — per-state action descriptions with their confirmation consequences, outcome messages derived from what the backend reports, provenance labels, and the `"500+"` awaiting-count formatter. `ProductUserRecord` carries `access` alongside `standing`, so every existing read, the standing flip, and the queue all expose it through the same explicit projections. The admin server's reader (`apps/admin/server/product-user-directory.ts`) gains `listProductUserAccessQueue`, `countAwaitingReview`, and `decideProductUserAccess` on the same authenticated POST integration, with all failure modes collapsed to the existing stable codes; decisions and records are validated field-by-field, an inconsistent effective-access verdict is refused rather than relayed, and the stored decision's operator/allowlist references are dropped at this boundary so nothing downstream can forward them. Five routes extend `server/routes/product-users.ts`: `/access/queue` and `/access/queue-count` guarded like the listing (`product_users:view` + same-origin), and `/access/{approve,decline,revoke}` guarded by `product_users:manage` + CSRF, POST-only with subjects in bodies (a query-string form 404s), `Cache-Control: no-store`, and explicit bounded response projections. The acting operator is always the session actor — the request schema rejects any body naming one. Every decision attempt is audited on the existing `audit_events` trail through the extended `server/product-user-audit.ts` sink (actions `product_user.{approve,decline,revoke}_access`, target pseudonymized via `createRecordReferencer`, previous/resulting decision state + provenance kind, `changed`, outcome, timestamp); a failed attempt is recorded before the refusal is reported, and a committed decision is reported even when its audit write fails (the established two-failure-domain pattern, reusing the same `onAuditFailure` report). The ledger (`ProductUserLedger`) shows access beside standing as two badges — Awaiting review is the pending tone and can never read as the danger-toned Suspended — plus a "Beta access" provenance fact ("Admitted automatically by the allowlist / Approved by an operator / Awaiting a first decision · date"). The Users page gains two parallel views: the full ledger and the review queue (oldest request first from the backend, bounded pages on the existing `KeysetPagination`, no search — the queue is worked, not searched), with the awaiting count in the page header (`role="status"` badge, "500+" when truncated) and on the queue toggle, refreshed after every decision. Approve/decline/revoke render per state (waiting → Approve/Decline; approved → Revoke; declined → Approve/"Return to review") from the row and the detail view, each through the existing confirm provider stating that person's consequence, updating the acted-on row in place with the decision the backend now holds — never reloading the listing under the operator — and reporting convergence honestly (an already-in-state repeat states the stored decision; approving a suspended account says they stay locked out). Data operators see badges and no controls anywhere and receive the standard 401/403 outcomes on direct requests; integration failure degrades to the established bounded non-destructive error state with no credential or upstream body in any browser payload; no subject, identifier, or fragment of one appears in any URL.
- Divergences: (1) `nothing_to_decide` is restated to the browser as the surface's existing 404 `PRODUCT_USER_NOT_FOUND` ("…nothing to decide") rather than a 200 outcome — identical to the standing flip's unknown-subject convention — and the attempt is still audited as a failure with that reason. (2) Not every action is offered on every row: each state offers the decisions that make sense there (the reference admin's single status control, generalized), and every state remains reversible — revoke-from-declined is surfaced as "Return to review" with its allowlist-readmission caveat spelled out, matching 003's generalized revoke. (3) The waiting count lives in the page header and queue toggle (the spec's page-header option) rather than the navigation: the admin has no nav-badge pattern and the layout fetches no data, so the convention was kept. (4) The queue view is a mode on the existing Users page rather than a separate route, so the two parallel views share the ledger, pagination, and states without a new URL surface; a decided row keeps its place with its new state and controls until the operator moves on, rather than vanishing mid-review. (5) The browser payload for a decision is `{action, changed, access, effectiveAccess}` — the resulting decision only; the previous decision travels to the audit trail, which is what needed it.
- Verification: `npm run lint:admin && npm run typecheck:admin && npm run test:admin && npm run build:admin` → all exit 0 (admin: 264 tests passed; 59 new or extended across `server/product-user-directory.test.ts` (7 new: queue read/ordering/cursor, count bounds, per-action endpoints, reference-dropping, broken-payload refusals, fail-closed unconfigured), `server/routes/product-users.behavior.test.ts` (10 new: view matrix on queue+count, oldest-first bounded queue projection, queue validation incl. POST-only, count relay with truncation, manage matrix per action incl. CSRF, session-stamped convergence incl. suspended-approval honesty, audit emission with decision movement, refused-decision audit + sanitization, nothing-to-decide 404 + audit, committed-decision-vs-unwritable-trail, validation incl. operator-naming rejection), `src/pages/ProductUsersPage.test.tsx` (badge distinctness and provenance, header count and 500+ display, queue view ordering/empty/truncated states, approve/decline/revoke confirmation copy and in-place row updates, convergence toast, cancel path, viewer absence in both views), and `src/pages/ProductUserDetailPage.test.tsx` (access badge/provenance/decide-in-place; viewer filter extended to the decision controls)). `npm run typecheck:contracts && npm run test:contracts` → exit 0 (172 passed; schemas, action sets, provenance/outcome copy). `npm run scan:framework-standards:ratchet` → 0 findings, 0 new. `node scripts/check-docs.mjs` → ok, 156 files. Layout smoke: the real page DOM (jsdom-rendered through the real components) with the real stylesheets checked in the browser for six states (directory, queue, empty queue, integration error, forbidden, decline confirmation) at 1280px and 375px in light and dark — `scrollWidth === clientWidth` in all 24 combinations, and visual passes confirm the pending-toned waiting badge beside the danger-toned suspended badge, the header count, the queue truncation note, and the confirmation dialog copy.
