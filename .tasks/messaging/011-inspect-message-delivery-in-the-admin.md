# Task: Inspect Message Delivery in the Admin

**ID:** messaging/011
**Depends on:** messaging/004, admin-tools/001
**Blocks:** messaging/012
**Estimated scope:** medium
**Status:** done

## Objective

Operators can answer "did that message actually go out?" from the admin — a searchable history of what was sent, to whom, when, through which provider, and what happened to it.

## Context

The moment PackScout starts sending mail, someone will ask whether a specific message arrived: an approved beta user says they never got the email, an operator did not receive an alert about an outage, an invited colleague never got their link. Without a delivery record the only answers are guesses and provider dashboard archaeology.

The queue from messaging/004 already records intents and attempts with outcomes, providers, error codes, and provider message identifiers. This task surfaces that, and adds the one operational affordance the queue cannot decide for itself: retrying something that failed terminally, once a human has fixed the underlying cause.

The care needed here is about restraint. A delivery log is a list of who was sent what — recipient addresses are personal data, and message bodies would be worse. The log shows enough to diagnose and no more.

## Requirements

- A Messages destination in the admin navigation, visible only to operators holding a named permission for viewing message delivery, and guarded server-side by it. The permission is granted to the administrator role only, following the existing permission vocabulary conventions.
- The history lists delivery intents with: the message kind, a bounded display of the recipient, current state, attempt count, when it was created and last attempted, the provider used, and the stable error code for failures.
- Ordering is by recency with bounded pagination; filtering covers at least state and message kind; search covers the recipient.
- An intent's detail shows its attempt history — each attempt's time, provider, outcome, stable error code, and the provider's message identifier on success — so a delivery can be correlated with the provider's own records.
- Message bodies are never displayed or stored for display. The log shows what kind of message it was, not what it said.
- Recipient addresses are shown because diagnosis requires them, but never travel in URLs, query strings, or browser history, and never appear in admin logs or metrics beyond audit-relevant identifiers.
- Counts of pending, retrying, and terminally failed intents are visible at a glance so a stuck queue is noticed rather than discovered.
- An operator holding a named permission for managing message delivery can retry a terminally failed intent. The retry is explicit, confirmed, bounded, and audited; it re-enters the normal queue rather than sending inline.
- Retrying an intent that is not terminally failed is refused, and concurrent retries of the same intent converge on one.
- Data operators receive the standard forbidden outcome for every route here and see no Messages navigation; anonymous requests receive the standard unauthenticated outcome.
- If the delivery records are unavailable, the area degrades to a clear, non-destructive error state; no credential or raw backend error reaches the browser.
- The area reuses the admin's existing template, ledger, filter, confirmation, toast, and empty-state patterns, and covers loading, empty, no-match, error, and forbidden states accessibly at desktop and narrow widths.

## User-Facing Behavior

An operator opens Messages, filters to failures, and sees that six approval emails failed with the same provider error code an hour ago. Opening one shows three attempts with backoff and the error each time. After the cause is fixed, they retry the failed ones and watch them move to sent. A glance at the header tells them whether anything is currently stuck.

## Interface Contract

- Protected admin endpoints for listing intents with filtering, search, and bounded pagination; reading one intent's attempt history; reading queue-state counts; and retrying a terminally failed intent — the first three guarded by the view permission and the retry by the manage permission.
- All of them read the intent and attempt records committed by messaging/004; this task adds no new delivery state of its own.
- Retry re-enqueues through messaging/004's normal path with its existing idempotency and claiming rules.
- Two named permissions are added to the existing vocabulary, granted to the administrator role only.

## Acceptance Criteria

- [x] Administrators can list, filter, search, and page delivery intents and open an intent's attempt history with provider, outcome, error code, and provider message identifier.
- [x] No message body is displayed anywhere, and recipient addresses never appear in URLs, query strings, or browser history.
- [x] Pending, retrying, and terminally failed counts are visible without paging the list.
- [x] A terminally failed intent can be retried with confirmation; the retry is audited, bounded, and re-enters the queue rather than sending inline.
- [x] Retrying a non-terminal intent is refused, and concurrent retries converge on one.
- [x] Anonymous and data-operator requests receive the standard unauthenticated and forbidden outcomes, and data operators see no Messages navigation.
- [x] Loading, empty, no-match, error, and forbidden states render accessibly at desktop and narrow widths without page-level overflow.

## Verification

Admin route behavior tests prove the permission matrix for both permissions, listing with filtering, search and pagination bounds, attempt-history contents, count accuracy, retry outcomes including refusal for non-terminal intents and convergence under concurrency, audit emission, and sanitized failure mapping; page tests cover every state and assert no message body is rendered. The admin lint, typecheck, test, and build commands exit 0.

## Spec Compliance

- Related specs reviewed: `.tasks/messaging/_index.md` (delivery visibility is the third addition beyond the reference; "did they actually get it?" framing); messaging/004's contract — this task displays exactly the read model it committed (`PrismaEmailMessageOutboxRepository.countIntents/listIntents/getIntent/listAttempts`, the `pending|retrying|sent|skipped|failed` vocabulary, attempt records with provider, stable code, provider message id, and no retained body) and adds no delivery state of its own; closed-beta-access/009's Spec Compliance — the permission split, POST-body reads, audit sink, sanitized-failure, and page patterns are ported from it; `docs/admin-feature-baseline.md`, `docs/ui-layout-standard.md`.
- Alignment: `message_delivery:view` / `message_delivery:manage` are appended to the shared vocabulary and granted to `admin` only (`packages/contracts/src/auth.ts`), following the `beta_allowlist:*` naming; sessions derive permissions from role, so the grant is live without migration. `packages/contracts/src/message-delivery.ts` is the shared browser/server vocabulary (bounded strict request schemas, closed row shapes with no field a body could travel in, stable admin error codes), per the `beta-allowlist.ts` precedent. The one queue affordance the read model lacked is `requeueTerminalIntent(intentId, now)` on `packages/database/src/email-message-outbox-repository.ts`: a single guarded `UPDATE … WHERE state='failed'` that moves `failed → pending` due now, preserves the attempt counter and history (so the retry is bounded — the drain's claim increments as usual and a further failure rests terminal again), clears the claim, and returns the refreshed record or `null` — concurrent retries converge because only one UPDATE matches. Delivery stays with the worker's drain; nothing sends inline. `apps/admin/server/routes/messages.ts` guards `/list`, `/counts`, `/detail` with `message_delivery:view` and `/retry` with `message_delivery:manage` + CSRF + same-origin through the existing `createRequireSession` boundary; everything is POST-with-body (a query-string form 404s), every response is an explicit field-by-field bounded projection (the stored `input_json` is unreachable by construction and by projection) with `Cache-Control: no-store`, cursors are opaque server-issued base64url keyset positions, and every failure collapses to a stable code with fixed copy — no database error, credential, or address in any browser payload or log line. Retries append to the existing `audit_events` trail (`apps/admin/server/message-delivery-audit.ts`): acting operator, `message_delivery.retry`, the intent UUID as subject, a pseudonymized recipient reference keyed through the workspace secret (the 009 convention), kind, outcome, and a stable reason on refusal; a refused or failed retry is recorded before the refusal is reported, and a committed requeue is reported even when its audit write fails (bounded `message_delivery_audit_write_failed` line, nothing personal). The browser surface (`MessagesPage`, `MessageDetailPage`, `MessageDeliveryLedger`, `messages.css`, `api/messages.ts`) reuses the template: counts band (`admin-metrics`), filter form (state select, kind select, recipient search), card-row ledger with keyset pagination, attempts table scrolling inside its own region, existing confirm/toast/empty-state/restricted-state providers. Filter state lives in component state — never `searchParams` — so no filter can reach a URL or history; the detail route carries only the queue's opaque intent UUID (the `users/:handle` precedent). Navigation ("Messages", Workspace section) renders only with `message_delivery:view`. The runtime (`message-delivery-runtime.ts`) constructs the same repository over the same Prisma client the admin's other operational pages use, wired append-only in `app.ts`/`index.ts`.
- Divergences: (1) Recipient search is an exact full-address match — messaging/004's read model filters recipient by equality, and this task adds no second read path; the page's copy and a 3-character minimum state the semantics. (2) The kind filter is a select over the seven kinds the catalogue ships; the vocabulary stays open — an unlisted kind still lists and renders under its raw identifier, it is only not selectable until named in the admin-local label list. (3) The counts band tiles pending, retrying, failed, and sent; due/claimed/skipped/oldestDueAt travel in the counts response for later use without being tiled (the spec's minimum is the first three). (4) "Bounded" retry is the preserved attempt counter: each explicit retry grants exactly one more drain attempt, after which a retryable failure rests terminal again immediately rather than re-entering the backoff ladder. (5) A malformed intent id in the detail route reads as the not-found state — to an operator they are the same fact. (6) The retry confirmation uses the standard tier, not danger: nothing is destroyed; the consequence copy is explicit instead.
- Verification: `npm run lint:admin && npm run typecheck:admin && npm run test:admin && npm run build:admin && npm run typecheck:contracts && npm run test:contracts && npm run test:database` → exit 0 (admin: 264 passed, 28 new — 10 in `server/routes/messages.behavior.test.ts`, 2 in `server/message-delivery-audit.test.ts`, 7 in `src/pages/MessagesPage.test.tsx`, 8 in `src/pages/MessageDetailPage.test.tsx`, 1 in `src/App.test.tsx`; contracts: 172 passed, 5 new; database: 155 passed, 2 new integration tests). Route tests prove the permission matrix for both permissions (anonymous 401, data operator 403, cross-origin 403, missing CSRF 403, admin 200), body-only filters (query-string form 404s), bounded validation and cursor refusal, the explicit projection under poisoned queue rows carrying `input_json`/body/secret markers (closed key-set assertions, marker regexes), attempt-history contents including `providerMessageId`, count accuracy, retry outcomes (success re-enters the queue due now; non-terminal 409 with the state named from the closed vocabulary; vanished 404), audit emission for allowed, refused, and failed retries plus the unwritable-trail path, and sanitized 503s that leak no connection string. Database tests prove failed→pending due-now requeue with preserved history, re-claimability by the ordinary drain, terminal-again-after-one-more-failure boundedness, refusal of pending/sent/skipped/unknown, and convergence of two concurrent requeues on exactly one. Page tests cover loading/empty/no-match/error/forbidden, the no-body assertion under smuggled body-like fields on both pages, recipient search traveling in the body with the page address unchanged, local refusal of fragment searches, the explicit retry confirmation wording, refusal reporting with reload, and the absence of the retry control without the manage permission or for non-failed intents. `npm run scan:framework-standards:ratchet` → 0 findings, 0 new. `node scripts/check-docs.mjs` → ok, 156 files. Layout smoke: real page DOM (jsdom-rendered with stubbed data) under the real stylesheets checked in the browser at 1280px and 375px in light and dark for list, error, detail, and retry-confirmation states — `scrollWidth === clientWidth` everywhere after fixing one real finding (the detail page's nowrap recipient heading refused to shrink inside the shared flex heading and widened the page at 375px; capped with `min-width: 0; max-width: 100%` and no flex-basis, since the shared heading becomes a column at narrow widths where a basis would read as height).
